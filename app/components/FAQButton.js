import { Button } from '@nextui-org/react'

export default function FAQButton () {
  return (
    <Button
      isIconOnly
      radius='full'
      variant='bordered'
      size='sm'
      fullWidth
      className='text-white border border-opacity-20'
    >?
    </Button>
  )
}
